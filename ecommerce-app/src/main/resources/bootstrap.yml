spring.application.name: spring-vault-demo
spring:
  cloud:
    vault:
      authentication: TOKEN
      token: s.lg7908gYgHVK6x4bEmeRYARe
      host: localhost
      port: 8200
      scheme: http
      database:
        enabled: true
        role: order
        backend: database
